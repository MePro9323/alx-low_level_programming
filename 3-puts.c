#include "main.h"
void _puts(char *str)
#include <stdio.h>
#include <stdlib.h>
#include <ctype.h>
#include <string.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */

void _puts(char *s)
{
	_puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
